# Headers added to scanned messages (V4 §41): the mail core and the
# platform read the verdict and signals from these.
use = ["x-spam-status", "x-spam-level", "x-rspamd-server", "x-rspamd-queue-id", "authentication-results"];
extended_spam_headers = true;
